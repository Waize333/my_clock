# Neon PostgreSQL. Keep all variables in this file server-only.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
# Generate a secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
# Optional Google OAuth. Callback: <BETTER_AUTH_URL>/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
